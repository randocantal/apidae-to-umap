name: Actualisation Donnees Apidae MULTI

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du depot
        uses: actions/checkout@v4

      - name: Configuration de Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Installation des dependances
        run: pip install requests

      - name: Execution du script de conversion
        env:
          APIDAE_API_KEY: ${{ secrets.APIDAE_API_KEY }}
          APIDAE_PROJECT_ID: ${{ secrets.APIDAE_PROJECT_ID }}
        run: |
          python -c "
          import os, requests, json
          
          api_key = os.environ.get('APIDAE_API_KEY', '').strip()
          project_id = os.environ.get('APIDAE_PROJECT_ID', '').strip()
          
          url = 'https://apidae-tourisme.com'
          headers = {'User-Agent': 'Mozilla/5.0'}
          
          config_selections = {
              'hotels': '188585',       
              'campings': '188587',     
              'gites_etape_sejour': '188586',        
              'chambres_hotes': '188589',     
              'villages_vacance': '188594',     
              'refuges': '188593'     
          }
          
          for nom_fichier, id_sel in config_selections.items():
              print(f'Extraction de la selection {id_sel}...')
              
              query_structure = {
                  'apiKey': api_key,
                  'projetId': int(project_id) if project_id.isdigit() else project_id,
                  'selectionIds': [int(id_sel)],
                  'responseFields': ['@all'],
                  'count': 600
              }
              
              try:
                  response = requests.get(url, params={'query': json.dumps(query_structure)}, headers=headers, timeout=45)
                  if response.status_code != 200:
                      continue
                  data = response.json()
              except:
                  continue
                  
              objets = data.get('objetsTouristiques', []) if data.get('objetsTouristiques') else data.get('members', [])
              geojson = {'type': 'FeatureCollection', 'features': []}
              
              for item in objets:
                  localisation = item.get('localisation', {})
                  geoloc = localisation.get('geolocalisation', {}) if localisation else {}
                  
                  if geoloc and geoloc.get('valide') is True:
                      geo_json_data = geoloc.get('geoJson', {}) if geoloc else {}
                      pt = geo_json_data.get('coordinates', []) if geo_json_data else []
                      
                      if pt and len(pt) == 2:
                          adresse_complete = 'Non renseignée'
                          tel = 'Non renseigné'
                          email = 'Non renseigné'
                          web = 'Non renseigné'
                          photo_url = 'https://website-files.com'
                          
                          # 1. Extraction de l'adresse
                          try:
                              adr_data = localisation.get('adresse', {})
                              if adr_data:
                                  lignes = [adr_data.get('adresse1'), adr_data.get('adresse2'), adr_data.get('adresse3')]
                                  adresse_txt = ', '.join([str(l) for l in lignes if l])
                                  cp_ville = f\"{adr_data.get('codePostal', '')} {adr_data.get('commune', {}).get('nom', '')}\".strip()
                                  if cp_ville:
                                      adresse_complete = f\"{adresse_txt}, {cp_ville}\" if adresse_txt else cp_ville
                          except:
                              pass
                          
                          # 2. Extraction des contacts
                          try:
                              moyens = item.get('moyensCommunication', [])
                              if not moyens:
                                  moyens = item.get('informations', {}).get('moyensCommunication', [])
                              
                              if moyens and isinstance(moyens, list):
                                  for m in moyens:
                                      type_id = m.get('type', {}).get('id')
                                      valeur = m.get('coordonnees', {}).get('fr', '').strip()
                                      if type_id == 201:
                                          tel = valeur
                                      elif type_id == 204:
                                          email = valeur
                                      elif type_id == 205:
                                          web = valeur
                          except:
                              pass
                          
                          # 3. Extraction de la photo
                          try:
                              illustrations = item.get('illustrations', [])
                              if illustrations and isinstance(illustrations, list) and len(illustrations) > 0:
                                  first_img = illustrations[0]
                                  trad = first_img.get('traductionFr', {})
                                  if trad and trad.get('url'):
                                      photo_url = trad.get('url')
                              if photo_url == '':
                                  multimedias = item.get('multimedias', [])
                                  if multimedias and isinstance(multimedias, list) and len(multimedias) > 0:
                                      first_multi = multimedias[0]
                                      trad = first_multi.get('traductionFr', {})
                                      if trad and trad.get('url'):
                                          photo_url = trad.get('url')
                          except:
                              pass
                          
                          # Reconstruction et correction des coordonnées géographiques [index 0 et 1]
                          try:
                              lon = float(pt[0])
                              lat = float(pt[1])
                              
                              geojson['features'].append({
                                  'type': 'Feature',
                                  'geometry': {'type': 'Point', 'coordinates': [lon, lat]},
                                  'properties': {
                                      'name': item.get('nom', {}).get('libelleFr', 'Sans nom'),
                                      'description': item.get('presentation', {}).get('descriptifCourt', {}).get('libelleFr', 'Aucune description disponible.'),
                                      'adresse': adresse_complete,
                                      'telephone': tel,
                                      'email': email,
                                      'web': web,
                                      'photo': photo_url
                                  }
                              })
                          except:
                              continue
              
              with open(f'{nom_fichier}.geojson', 'w', encoding='utf-8') as f:
                  json.dump(geojson, f, ensure_ascii=False, indent=2)
              print(f'Fichier {nom_fichier}.geojson genere avec {len(geojson[\"features\"])} points.')
          "

      - name: Sauvegarder les modifications sur GitHub
        run: |
          git config --global user.name 'Apidae Bot'
          git config --global user.email 'bot@github.com'
          git add *.geojson
          git commit -m "Correction definitive indexation des coordonnées, contacts et photos" || echo "Aucune modification"
          git push origin HEAD:main || git push --force origin HEAD:main
